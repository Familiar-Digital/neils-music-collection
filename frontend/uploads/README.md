Artwork uploaded from the app.

One file per record, named by its spreadsheet row, so re-uploading replaces
rather than accumulating. Served from raw.githubusercontent.com rather than the
published site: committing here triggers a Pages rebuild that takes the better
part of a minute, and an image served from the site would appear broken to
anyone who looked straight after uploading.
